# Scripts

This directory is reserved for reproducibility helpers such as:

- environment capture
- Paddle and PaddleX patch export
- ROCm repro commands
- benchmark runners
- remote Jupyter Lab API helpers
- remote Jupyter terminal websocket helpers
- remote bootstrap command generators
- remote environment check command generators
- remote per-instance preparation wrappers